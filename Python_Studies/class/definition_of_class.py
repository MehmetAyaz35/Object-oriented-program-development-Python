class MyClass:           #In Python, "self" is a convention used as the first parameter in the methods of a class.
                         # It represents the instance of the class itself. When you create an instance of a class,
                         # methods within that class can access and manipulate the instance's attributes and state through the "self" parameter.
    def some_method(self, parameter1, parameter2):   #An argument is a value or an expression that you provide when calling a function or method. It's the actual data that you pass into the function's parameters.
        # Access instance attributes using self
        self.attribute = parameter1
        # Do something with parameter2
