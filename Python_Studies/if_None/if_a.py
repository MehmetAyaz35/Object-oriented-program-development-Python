a = 0
if a:                            # if a true ise
    print("a is not falsy")
else:                            # if a false ise
    print("a is falsy")

# if a:
# This checks if the value of A evaluates to True in a boolean context. In Python, certain values are considered "truthy" and others "falsy." For example, 0, an empty list ([]), an empty string (""), and None are considered falsy.
    
# in conclusion while "if a" checks whether a variable is true or not "if a is not None:" checks if the variable has a value(not None) 